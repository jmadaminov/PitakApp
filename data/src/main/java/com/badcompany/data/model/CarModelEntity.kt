package com.badcompany.data.model

/**
 * Representation for a [CarModelEntity] fetched from an external layer data source
 */
data class CarModelEntity(val id: Int,
                          val name: String)